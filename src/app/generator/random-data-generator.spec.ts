import { RandomDataGenerator } from './random-data-generator';

describe('RandomDataGenerator', () => {
  it('should create an instance', () => {
    expect(new RandomDataGenerator()).toBeTruthy();
  });
});
